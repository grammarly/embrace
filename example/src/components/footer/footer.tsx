import * as React from 'react'
import { UI } from '@grammarly/embrace'

import './footer.css'

export const Footer = UI.Node.make<never, never>(() => (
  <footer className="footer">replace me</footer>
))
